<!DOCTYPE html>
<html lang='en'>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>DashboardServer</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class='container'>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="nav-item dropdown">
          <a class="nav-brand dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            PDX Hackerspace Dashboard
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
<% if current_user && can?(:manage, Device) %>
	    <%= link_to 'Devices', devices_path, class: 'dropdown-item' %>
<% end %>
<% if true || current_user && can?(:manage, Template) %>
	    <%= link_to 'Templates', templates_path, class: 'dropdown-item' %>
<% end %>
<% if current_user && can?(:manage, User) %>
	    <%= link_to 'Users', users_path, class: 'dropdown-item' %>
<% end %>
<% if false %>
	    <%= link_to 'Reports', reports_path, class: 'dropdown-item' %>
<% end %>
          </div><!-- .dropdown-menu -->
      </div><!-- .nav-item-dropdown -->

      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
	  <li class="nav-item">
<% if user_signed_in? %>
	    <a class="nav-link" href="#">Logout</a>
<% else %>
	    <%= link_to 'LoginX', '/auth/slack', method: :post %>
<% end %>
	  </li>
        </ul>
      </div><!-- .collapse -->
    </nav>

    <%= yield %>
    </div>
  </body>
</html>
